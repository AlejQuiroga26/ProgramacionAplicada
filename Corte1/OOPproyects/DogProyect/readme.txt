Class Dog

Especies = "Familiares caninos" #Se crea un atributo, con el valor String en comillas

def __init__(self, nombre, edad): #Se delcaran los parametros del método
        self.nombre = nombre #crea un atributo llamado nombre y le asigna el valor del parámetro.nombre
        self.edad = edad #crea un atributo llamado edad y le asigna el valor del parámetro.edad

miles = Dog() #Declaracio del objeto
miles = Dog("Miles", 4) #Se crea el objeto Miles y se 
buddy = Dog("Buddy", 9) #Se crea el objeto Buddy
